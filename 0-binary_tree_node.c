binary_tree_t *binary_tree_node(binary_tree_t *parent, int value)
{

    if (result != NULL)
    return (NULL);
    {
        result->left = NULL;
        result->right = NULL;
        result->n = value;
        result->parent = parent;
    }
    return (result);
}
